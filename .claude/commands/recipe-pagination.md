Run the Packmind recipe "Add Pagination to a List Page".

First, fetch the recipe details using:
```
mcp__packmind__get_recipe_details(recipeSlug: "add-pagination-to-a-list-page")
```

Then guide me through implementing pagination by:
1. Asking the context validation checkpoint questions to understand my requirements
2. Following each step of the recipe with code examples adapted to my specific list
3. Ensuring all Packmind standards are followed during implementation

Help me add pagination to: $ARGUMENTS