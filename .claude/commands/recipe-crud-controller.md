Run the Packmind recipe "Add a CRUD Controller Action in Symfony".

First, fetch the recipe details using:
```
mcp__packmind__get_recipe_details(recipeSlug: "add-a-crud-controller-action-in-symfony")
```

Then guide me through implementing a CRUD controller action by:
1. Asking the context validation checkpoint questions to understand my requirements
2. Following each step of the recipe with code examples adapted to my specific entity/use case
3. Ensuring all Packmind standards are followed during implementation

Help me implement: $ARGUMENTS
