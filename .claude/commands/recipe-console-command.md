Run the Packmind recipe "Add a Console Command in Symfony".

First, fetch the recipe details using:
```
mcp__packmind__get_recipe_details(recipeSlug: "add-a-console-command-in-symfony")
```

Then guide me through creating a console command by:
1. Asking the context validation checkpoint questions to understand my requirements
2. Following each step of the recipe with code examples adapted to my specific command
3. Ensuring all Packmind standards are followed during implementation

Help me create command: $ARGUMENTS
