Run the Packmind recipe "Add a Custom Form Field Type".

First, fetch the recipe details using:
```
mcp__packmind__get_recipe_details(recipeSlug: "add-a-custom-form-field-type")
```

Then guide me through creating a custom form field by:
1. Asking the context validation checkpoint questions to understand my requirements
2. Following each step of the recipe with code examples adapted to my specific field type
3. Ensuring all Packmind standards are followed during implementation

Help me create custom field: $ARGUMENTS